[package]
name = "scribble_canvas"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
# Graphics: winit for windowing, wgpu or gl for rendering
# Placeholder: simple stubs for now
